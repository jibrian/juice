/**
* App Controller
*/
define(["marionette"],
	function(Marionette) {
		var AppController = Marionette.Controller.extend({
			initialize: function(options) {

			}
		});

		return AppController;
	}
); // !define