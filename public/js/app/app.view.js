/**
* App View
*/
define(["marionette"]
	function(Marionette) {
		var AppView = Marionette.ItemView.extend({
			el: "#app",
			initialize: function(options) {

			}
		});

		return AppView;
	}
); // !define